package com.example.ashish.task.adapter;

import com.example.ashish.task.model.RowData;

import java.util.ArrayList;

/*
public class RepositoriesListPresenter {

    private final ArrayList<RowData> list = new ArrayList();


    public void onBindRepositoryRowViewAtPosition(int position, GetDataInterface.RowView rowView) {
        RowData data = list.get(position);
        rowView.setTitle(data.getTitle());
        rowView.setDescription(data.getDescription());
        rowView.setImage(data.getImageHref());
    }

    public int getRepositoriesRowsCount() {
        return list.size();
    }

}*/
